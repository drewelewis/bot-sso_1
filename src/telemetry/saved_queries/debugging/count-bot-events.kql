// Count Bot Telemetry Events
// Shows total number of telemetry events generated by your bot
// This includes custom events, bot messages, and internal operations
dependencies
| where type == "InProc" and name != ""
| count
