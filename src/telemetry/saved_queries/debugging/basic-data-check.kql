// Check What Data You Have
// Basic data exploration for OpenTelemetry implementation
// Shows recent traces with key fields for debugging
traces 
| where timestamp > ago(1h)
| project timestamp, message, severityLevel, customDimensions
| take 20
