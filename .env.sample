# Azure Cosmos DB Configuration
# Replace these values with your actual Cosmos DB settings

# Cosmos DB endpoint URL (e.g., https://your-cosmosdb-account.documents.azure.com:443/)
COSMOS_DB_ENDPOINT=https://your-cosmosdb-account.documents.azure.com:443/

# Authentication method - choose one:
# Option 1: Use AAD authentication (recommended for production)
COSMOS_DB_USE_AAD=true

# Option 2: Use primary key authentication (if local auth is enabled)
# COSMOS_DB_AUTH_KEY=your-cosmos-db-primary-key

# Database name (will be created if it doesn't exist)
COSMOS_DB_DATABASE_ID=bot-storage

# Container name (will be created if it doesn't exist)
COSMOS_DB_CONTAINER_ID=bot-state

# Other existing environment variables
# ... (keep your existing environment variables)
